using c

h{*}: extension = h
c{*}: extension = c

cxx.std = latest
using cxx

hxx{*}: extension = h
cxx{*}: extension = cpp

test.target = $c.target

exe{*}: test = true

import lib = stb_rect_pack%lib{stb_rect_pack}
